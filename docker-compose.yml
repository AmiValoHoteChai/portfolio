version: '3.8'

services:
  portfolio:
    image: nginx:alpine
    container_name: personal_portfolio
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./public:/usr/share/nginx/html:ro
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
